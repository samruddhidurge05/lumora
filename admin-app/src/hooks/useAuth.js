export { useAuth } from '../context/AuthContext';

// Default export alias — supports: import useAuth from '../../hooks/useAuth'
import { useAuth } from '../context/AuthContext';
export default useAuth;
